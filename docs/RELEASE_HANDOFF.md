# Release handoff

The engineering branch contains the current ADTrans RealIndo enterprise operating-system implementation. See `docs/FINISH_GATE.md` for the exact boundary between implemented controls and external operational activation.
