// Single public production entry. Vite bundles the portfolio, boundary guard,
// and branding together so the browser never executes raw source asset imports.
import './public-site.js';
import './public-interface-guard.js';
import './brand-enhancer.js';
